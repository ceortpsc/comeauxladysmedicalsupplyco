import Link from "next/link";
import { PROGRAMS } from "@comeaux/training";

export default function AcademyPage(){return <section className="section"><div className="eyebrow">Healthcare education</div><h1>Comeaux Academy</h1><p className="lead">Distance-learning theory, human-instructor oversight, skills-lab gates, structured remediation and audit-ready completion evidence.</p><div className="grid">{PROGRAMS.map(program=><article className="card" key={program.code}><span className="badge">{program.status}</span><h3>{program.name}</h3><p>{program.summary}</p><p><strong>{program.hours}</strong> structured hours • {program.modules} modules</p><Link href="/lms">Open LMS</Link></article>)}</div><div className="notice" style={{marginTop:"1.5rem"}}>Program publication does not equal HHSC approval, permit eligibility, exam authorization or credential issuance. Those statuses are external regulatory events and must be recorded only after actual confirmation.</div></section>}
