import { PROGRAMS, LEARNING_ENGINE } from "@comeaux/training";
import { createScriptedScenario } from "@comeaux/ai-core";

export default function LmsPage(){const sample=createScriptedScenario("resident-rights");return <section className="section"><div className="eyebrow">Learning management system</div><h1>Human-seeded, AI-assisted LMS</h1><div className="grid"><article className="card"><h3>Learning engine</h3><p>{LEARNING_ENGINE.join(" • ")}</p></article><article className="card"><h3>Grading</h3><p>Knowledge checks, quizzes, assignments, case studies, cumulative exams, attendance clocks, remediation and instructor competency sign-off.</p></article><article className="card"><h3>Simulation</h3><p>{sample.prompt}</p><p><strong>Expected action:</strong> {sample.expected}</p></article></div><div className="list" style={{marginTop:"1.5rem"}}>{PROGRAMS.map(p=><div className="listItem" key={p.code}><strong>{p.name}</strong><div>{p.modules} modules • {p.hours} hours • state status: {p.status}</div></div>)}</div></section>}
